version: '3.8'

services:
  app:
    build: .
    ports:
      - "8008:8008"
    environment:
      - PORT=8008
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis
    volumes:
      - ./data:/app/data

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
